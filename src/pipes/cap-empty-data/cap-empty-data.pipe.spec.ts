import { CapEmptyDataPipe } from './cap-empty-data.pipe';

describe('CapEmptyDataPipe', () => {
  it('create an instance', () => {
    const pipe = new CapEmptyDataPipe();
    expect(pipe).toBeTruthy();
  });
});
