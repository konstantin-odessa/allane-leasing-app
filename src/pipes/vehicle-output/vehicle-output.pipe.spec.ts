import { VehicleOutputPipe } from './vehicle-output.pipe';

describe('VehicleOutputPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleOutputPipe();
    expect(pipe).toBeTruthy();
  });
});
